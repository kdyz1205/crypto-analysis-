// frontend/js/ui/boot_status.js — disabled (was causing header overlap)
// Boot status badges removed from UI. Functions kept as no-ops so callers don't break.

export function initBootStatus() {}
export function markBoot(_key, _state, _detail) {}
export function getBootState() { return {}; }
