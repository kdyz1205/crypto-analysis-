// Stage 5 placeholder. Order/position markers arrive in Stage 6 after paper trading exists.

export function clearOrderOverlay(_chart) {}

export function drawOrderOverlay(_chart, _snapshot, _layerVisibility = {}) {
  return [];
}
